# RunPod
runpod>=1.6.0

# Core ML libraries - versions for Flux support
# Flux requires torch.nn.RMSNorm (2.4.0+) and enable_gqa parameter (2.5.0+)
torch>=2.5.0
torchvision>=0.20.0
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.3

# Quantization support for FLUX.2 (4-bit NF4)
bitsandbytes>=0.43.0

# Image processing
Pillow>=10.0.0

# Optional optimizations
sentencepiece>=0.2.0
protobuf>=3.20.0

# Utilities
pydantic>=2.0.0
